
        #     min_similarity = word_length
        #     for word in valid_words:
        #         similarity = sum(1 for g, w in zip(guesses, word) if g != w)
        #         if similarity < min_similarity:
        #             min_similarity = similarity
        #             my_word = word